# 2026-07-14 公告组件恢复+名片辉光效果

## 操作记录

### 1. 恢复公告组件为 Firefly 原版
- 删除自定义的 ElectricBorder 电光边框（Canvas 1px 蓝色 #3B82F6 噪点线条）
- 删除 ElectricBorder.tsx、ElectricBorderWrapper.astro、ElectricBorder.css（全量移除）
- 恢复 Announcement.astro 为上游 `CuteLeaf/Firefly` master 分支原版：
  - 纯 `WidgetLayout` 包裹，无额外效果
- 保留原版的链接按钮和关闭按钮

### 2. 套用名片 glow-card 辉光跟随鼠标效果
- 参考 `Profile.astro` 的 `glow-card` 结构：
  - 外层 div `glow-card`（`position:relative; isolation:isolate`）
  - `<span class="edge-light"></span>` 边缘光
  - 内层 div `glow-card-inner` 包裹 WidgetLayout
- 复制整套 CSS：
  - 伪元素 `::before` / `::after` 多层径向渐变 + conic-gradient mask
  - `edge-light` 的 box-shadow 辉光层
  - CSS 变量 `--edge-proximity` / `--cursor-angle` 控制辉光位置和强度
- 复制 JS 鼠标跟随逻辑（`pointermove` 事件计算边缘距离和角度）

### 3. 修复问题
- **公告内容无换行** — 原版上游缺 `white-space: pre-line`，加上后 `\n` 正常换行
- **公告卡片纯透明** — `glow-card-inner` 没有背景色，加上 `background-color: var(--card-bg-transparent, rgba(255,255,255,0.6))` + `backdrop-filter: blur(16px)`，与名片完全一致

### 4. Git 提交记录
- `7260b49d` — `chore: 公告组件恢复Firefly原版+辉光跟随鼠标效果，删除ElectricBorder组件`
- `2968dbbf` — `fix: 公告卡片底色设为与名片一致的半透明背景 (--card-bg-transparent)`

## 关键技术点
- Firefly 原版公告组件路径：`src/components/widget/Announcement.astro`
- 辉光效果来源于 Profile.astro 的 glow-card CSS + JS
- `--card-bg-transparent` 为动态 CSS 变量，运行时由 JS 设置（基于 `--card-transparent-opacity`）
- 注意：WidgetLayout 自带 `<span class="edge-light"></span>`，外层 glow-card 也带一个，有两个 edge-light 共存
